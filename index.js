/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MetaImagem from './MetaImagem';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MetaImagem);
